{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build LibrarySystem",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/Book.cpp",
                "${workspaceFolder}/Member.cpp",
                "${workspaceFolder}/Library.cpp",
                "${workspaceFolder}/main.cpp",
                "-o",
                "${workspaceFolder}/LibrarySystem.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}